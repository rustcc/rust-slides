## Example of using wasm-bindgen in browser with ES modules

### Building

```
wasm-pack build --target web
```

### Running

Serve statics

```
http
# or, alternatively
python -m SimpleHTTPServer
```

Then navigate to `localhost:port`
